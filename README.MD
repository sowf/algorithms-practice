## Some data-structures and algorithms implemetation

Repo is build for algorithms that can't be practiced on leetcode.

## Installation

```sh
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
